export class PitchPlayerCard {
    constructor()
    constructor(id: string, name: string, position: string, rating: number, rarity: string)
    constructor(public id?: string, public name?: string, public position?: string, public rating?: number, public rarity?: string) {

    }
}